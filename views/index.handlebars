<h1>Eat-Da-Burger!</h1>

<img src="/assets/img/burger.png" alt="cartoon burger">

<h2>Order Up!</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      <li>{{burger_name}}<button class="devour" data-id={{id}}>Devour</button></li>
    {{/unless}}
  {{/each}}
</ul>

<h2>That was Delicious!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
      <li>{{burger_name}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Order a Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="order">Enter the kind of Burger you would like:</label>
    <input type="text" id="order" name="name">
  </div>

  <button type="submit">Order</button>
</form>
